<Window x:Class="StageManagementSystem.Views.AddStudentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StageManagementSystem.Views"
        mc:Ignorable="d"
        Title="Nieuwe Student Toevoegen" Height="650" Width="600"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource Gray50}">
    <Border Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="➕ Nieuwe Student Toevoegen" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Command="{Binding SelectProfilePictureCommand}" Width="64" Height="64" Style="{StaticResource BtnSecondary}" Padding="0" ToolTip="Kies Profielfoto" Background="Transparent" BorderThickness="0">
                    <Grid>
                        <Border CornerRadius="32" Width="64" Height="64" Background="{DynamicResource Gray200}" BorderBrush="{DynamicResource Gray300}" BorderThickness="1"/>
                        <TextBlock Text="📷" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource Gray600}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ProfilePicturePath}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ProfilePicturePath}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <Border CornerRadius="32" Width="64" Height="64">
                            <Border.Background>
                                <ImageBrush ImageSource="{Binding ProfilePicturePath}" Stretch="UniformToFill"/>
                            </Border.Background>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ProfilePicturePath}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ProfilePicturePath}" Value="">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                    </Grid>
                </Button>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
                <StackPanel>
                    <!-- Name & Number -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Voornaam *" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding FirstName}" Padding="8"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Achternaam *" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding LastName}" Padding="8"/>
                        </StackPanel>
                    </Grid>
                    
                    <StackPanel Margin="0,0,0,16">
                         <TextBlock Text="Studentnummer" FontWeight="SemiBold" Margin="0,0,0,4"/>
                         <TextBox Text="{Binding StudentNumber}" Padding="8"/>
                    </StackPanel>

                    <!-- Contact Info -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding Email}" Padding="8"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Telefoon" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding Phone}" Padding="8"/>
                        </StackPanel>
                    </Grid>

                    <!-- Type & Role -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Type Traject *" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <ComboBox SelectedValue="{Binding Type}" SelectedValuePath="Content" Padding="8">
                                <ComboBoxItem Content="stage"/>
                                <ComboBoxItem Content="scriptie"/>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Mijn Rol" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <ComboBox SelectedValue="{Binding MyRole}" SelectedValuePath="Content" Padding="8">
                                <ComboBoxItem Content="docentbegeleider"/>
                                <ComboBoxItem Content="1e examinator"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Bedrijf *" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Company}" Padding="8"/>
                    </StackPanel>
                    
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Locatie" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Location}" Padding="8"/>
                    </StackPanel>

                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Startdatum *" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <DatePicker SelectedDate="{Binding StartDate}" Padding="4"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Einddatum *" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <DatePicker SelectedDate="{Binding EndDate}" Padding="4"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Annuleren" IsCancel="True" Style="{StaticResource BtnSecondary}" Margin="0,0,10,0" Click="Cancel_Click"/>
                <Button Content="Student Toevoegen" Style="{StaticResource BtnPrimary}" Click="Save_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
