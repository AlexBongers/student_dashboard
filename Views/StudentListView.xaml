<UserControl x:Class="StageManagementSystem.Views.StudentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StageManagementSystem.Views"
             xmlns:helpers="clr-namespace:StageManagementSystem.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">
    <Border Style="{StaticResource CardStyle}" Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border BorderBrush="{DynamicResource Gray100}" BorderThickness="0,0,0,1" Padding="20,16">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="👥 Studenten" FontSize="18" FontWeight="Bold"/>
                    <TextBlock Foreground="{DynamicResource Gray600}" VerticalAlignment="Center" Margin="5,0,0,0">
                        <Run Text="("/>
                        <Run Text="{Binding Students.Count, Mode=OneWay}"/>
                        <Run Text=")"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Filters -->
            <Border Grid.Row="1" Padding="16,0,16,16">
                <StackPanel>
                    <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource Gray200}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,12" Padding="8,4">
                         <Grid>
                             <Grid.ColumnDefinitions>
                                 <ColumnDefinition Width="Auto"/>
                                 <ColumnDefinition Width="*"/>
                             </Grid.ColumnDefinitions>
                             <TextBlock Text="🔍" Margin="4,0,8,0" VerticalAlignment="Center" Foreground="{DynamicResource Gray600}"/>
                             <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                                      Grid.Column="1" BorderThickness="0" Background="Transparent" 
                                      VerticalContentAlignment="Center" helpers:Placeholders.Text="Zoeken..."/>
                         </Grid>
                    </Border>
                    
                    <CheckBox Content="Toon gearchiveerde studenten" IsChecked="{Binding ShowArchived}" Margin="4,0,0,12" Foreground="{DynamicResource Gray600}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <RadioButton Content="Alle" GroupName="Filter" IsChecked="{Binding FilterType, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=all}" 
                                         Style="{StaticResource TabButtonStyle}" Margin="0,0,8,0"/>
                            <RadioButton Content="Stage" GroupName="Filter" IsChecked="{Binding FilterType, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=stage}" 
                                         Style="{StaticResource TabButtonStyle}" Margin="0,0,8,0"/>
                            <RadioButton Content="Afstudeer" GroupName="Filter" IsChecked="{Binding FilterType, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=scriptie}" 
                                         Style="{StaticResource TabButtonStyle}"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="📦 Archiveren" Command="{Binding BulkArchiveCommand}" Style="{StaticResource BtnSecondary}" Padding="12,4" ToolTip="Archiveer geselecteerde studenten"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- List -->
            <ListBox Grid.Row="2" ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent, Mode=TwoWay}" 
                     BorderThickness="0" Background="Transparent" Padding="16,0,16,16"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Margin" Value="0,0,0,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource Gray200}" BorderThickness="1" CornerRadius="12" Padding="12">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryColor}"/>
                                            <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SelectionBackgroundBrush}"/> 
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryColor}"/>
                                            <Setter Property="Cursor" Value="Hand"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Bulk Checkbox -->
                            <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            
                            <!-- Avatar / Initials -->
                            <Border Grid.Column="1" Width="40" Height="40" CornerRadius="20" Background="{DynamicResource Gray100}" Margin="0,0,12,0" VerticalAlignment="Center">
                                <Grid>
                                    <TextBlock Text="👤" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ProfilePicturePath}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ProfilePicturePath}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <Border CornerRadius="20" Width="40" Height="40">
                                        <Border.Background>
                                            <ImageBrush ImageSource="{Binding ProfilePicturePath}" Stretch="UniformToFill"/>
                                        </Border.Background>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ProfilePicturePath}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ProfilePicturePath}" Value="">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                </Grid>
                            </Border>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="{DynamicResource Gray800}" FontSize="14"/>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock Text="{Binding Company}" Foreground="{DynamicResource Gray600}" FontSize="12" TextTrimming="CharacterEllipsis" MaxWidth="150"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <!-- Pill & Warning Badge -->
                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="⚠️" Foreground="{DynamicResource DangerColor}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" ToolTip="Urgent of Verlopen Deadline!">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasUrgentDeadline}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <Border Background="{DynamicResource Gray100}" CornerRadius="4" Padding="6,2">
                                    <TextBlock Text="{Binding Status}" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource Gray600}"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
